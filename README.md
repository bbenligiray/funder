# Funder

## Instructions

```sh
yarn
cp ./config/config.example.json ./config/config.json
NETWORK=localhost yarn deploy:local-node
NETWORK=localhost yarn deploy:funder
NETWORK=localhost yarn deploy:funder-depositories
NETWORK=localhost yarn fund
```
